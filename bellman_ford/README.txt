    #project name:
        Bellman Ford (vector protocal)

    #Background:
        python 2.7.10

    #How to run it:
        OS X:
            type "python [file_name]" on Terminal

    note: you would like put the input file and this program
          in same location

    #Contributing:
        1.find the routing table at every node
    **  2.show the result on screen
    
    note: we only support the node's name as number, not string or Alphabet

